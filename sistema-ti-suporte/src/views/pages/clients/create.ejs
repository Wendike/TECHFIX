<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <%- include('../../partials/head') %>
</head>
<body>
  <div class="app">
    <%- include('../../partials/sidebar') %>

    <main class="main-content">
      <%- include('../../partials/header') %>

      <% if (error && error.length) { %>
        <div class="alert alert-error">
          <% error.forEach(function(message) { %>
            <p><%= message %></p>
          <% }) %>
        </div>
      <% } %>

      <section class="panel">
        <div class="panel-header">
          <div>
            <h2>Novo Cliente</h2>
            <p>Cadastre o cliente para uso em ordens de serviço, dispositivos, vendas e financeiro.</p>
          </div>

          <a href="/clients" class="btn">Voltar</a>
        </div>

        <form method="POST" action="/clients" class="form-grid">
          <div class="form-group">
            <label>Usuário vinculado</label>
            <select name="user_id">
              <option value="">Nenhum usuário vinculado</option>
              <% clientUsers.forEach(function(userOption) { %>
                <option value="<%= userOption.id %>">
                  <%= userOption.name %> - <%= userOption.email %>
                </option>
              <% }) %>
            </select>
          </div>

          <div class="form-group">
            <label>Status</label>
            <select name="status">
              <% statuses.forEach(function(status) { %>
                <option value="<%= status.value %>"><%= status.label %></option>
              <% }) %>
            </select>
          </div>

          <div class="form-group">
            <label>Nome do cliente *</label>
            <input type="text" name="name" required>
          </div>

          <div class="form-group">
            <label>E-mail</label>
            <input type="email" name="email">
          </div>

          <div class="form-group">
            <label>Telefone</label>
            <input type="text" name="phone">
          </div>

          <div class="form-group">
            <label>CPF/CNPJ</label>
            <input type="text" name="document">
          </div>

          <div class="form-group">
            <label>Endereço</label>
            <input type="text" name="address">
          </div>

          <div class="form-group">
            <label>Cidade</label>
            <input type="text" name="city">
          </div>

          <div class="form-group">
            <label>Estado</label>
            <input type="text" name="state" maxlength="2" placeholder="SP">
          </div>

          <div class="form-group">
            <label>CEP</label>
            <input type="text" name="zip_code">
          </div>

          <div class="form-group form-full">
            <label>Observações</label>
            <textarea name="notes" rows="4"></textarea>
          </div>

          <div class="form-actions">
            <button type="submit" class="btn btn-primary">Salvar Cliente</button>
            <a href="/clients" class="btn">Cancelar</a>
          </div>
        </form>
      </section>
    </main>
  </div>

  <%- include('../../partials/scripts') %>
</body>
</html>
