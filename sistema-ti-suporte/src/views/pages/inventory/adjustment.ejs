<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <%- include('../../partials/head') %>
</head>
<body>
  <div class="app">
    <%- include('../../partials/sidebar') %>

    <main class="main-content">
      <%- include('../../partials/header') %>

      <% if (error && error.length) { %>
        <div class="alert alert-error">
          <% error.forEach(function(message) { %>
            <p><%= message %></p>
          <% }) %>
        </div>
      <% } %>

      <section class="panel">
        <div class="panel-header">
          <div>
            <h2>Ajuste de Estoque</h2>
            <p>Defina manualmente o saldo correto de uma peça após conferência física.</p>
          </div>

          <a href="/inventory" class="btn">Voltar</a>
        </div>

        <form method="POST" action="/inventory/adjustment" class="form-grid">
          <div class="form-group">
            <label>Peça *</label>
            <select name="part_id" required>
              <option value="">Selecione uma peça</option>
              <% parts.forEach(function(part) { %>
                <option value="<%= part.id %>" <%= Number(selectedPartId) === Number(part.id) ? 'selected' : '' %>>
                  <%= part.name %> | Estoque atual: <%= part.current_stock %>
                </option>
              <% }) %>
            </select>
          </div>

          <div class="form-group">
            <label>Novo estoque *</label>
            <input type="number" name="new_stock" min="0" required>
          </div>

          <div class="form-group form-full">
            <label>Motivo / Observação</label>
            <textarea name="reason" rows="4" placeholder="Ex: Conferência física, divergência, perda, correção manual..."></textarea>
          </div>

          <div class="form-actions">
            <button type="submit" class="btn btn-warning">Registrar Ajuste</button>
            <a href="/inventory" class="btn">Cancelar</a>
          </div>
        </form>
      </section>
    </main>
  </div>

  <%- include('../../partials/scripts') %>
</body>
</html>
